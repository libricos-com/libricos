/**
Ñapa eliminación de table of contents duplicada en el pods review
*/
window.onload = function(){
	/*
	var nodes = document.querySelectorAll("[id='toc_container']");
	if(nodes.length > 1){
		nodes[1].remove();
	}
	*/
};